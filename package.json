{
  "name": "petadopt",
  "version": "1.0.0",
  "description": "Plataforma de adopción de mascotas — proyecto de diseño web",
  "scripts": {
    "build": "sass --load-path=node_modules src/scss/app.scss dist/css/main.css --style=compressed --source-map",
    "watch": "sass --load-path=node_modules src/scss/app.scss dist/css/main.css --watch",
    "dev": "sass --load-path=node_modules src/scss/app.scss dist/css/main.css --watch"
  },
  "devDependencies": {
    "bootstrap": "^5.3.3",
    "sass": "^1.77.0"
  }
}
